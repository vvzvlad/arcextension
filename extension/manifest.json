{
  "manifest_version": 3,
  "name": "arcextension",
  "//version": "This literal is the BASE, not what ships. `instancegen bundle` (make bundle / make dev-bundle) stamps the BUILT copy's manifest with `version` = <major>.<minor>.<commit-count> — only the first two components are taken from here, the third is `git rev-list --count HEAD` — plus a `version_name` carrying that version, the short sha (with `-dirty` when the working tree has uncommitted changes) and the build time. `version_name` is what brave://extensions DISPLAYS, which is how you tell whether pressing «Обновить» on the extension card actually picked up the build you just made. Do NOT bump this literal per build: the commit count already moves, and the repo's own extension/ is never written by the build. Bump it only to move the major/minor line. If git is unavailable the build degrades to copying this manifest verbatim, so an unstamped bundle shows exactly the version below.",
  "version": "0.1.0",
  "description": "Curator client: reports tab activity snapshots and executes curator commands (§5, §6).",

  "//permissions": "`bookmarks` and `history` are READ-MOSTLY startpage permissions (§10): the newtab renders three columns — favourites (quick links + chrome.bookmarks), tabs, and history — and both are LOCAL sources, available on the very first paint with no network (offline-first). `bookmarks` is also written: the favourites column renames/removes/adds entries in place. They are NOT used by the service worker and nothing from them is ever reported to the curator service. Note for the operator: adding them WIDENS the install-time warning — Chrome now asks for «Читать и изменять закладки» and «Читать историю просмотров» on top of the tab permissions. Removing them does not break the extension; the two columns just render empty (the adapters tolerate a missing API, see startpage/src/lib/adapters.js).",
  "permissions": [
    "tabs",
    "scripting",
    "alarms",
    "storage",
    "idle",
    "bookmarks",
    "history"
  ],

  "//host_permissions": "ONLY <all_urls> now. The two per-host patterns (https://<host>/* and wss://<host>/*) are REMOVED: with enrollment (§7, issue #35) the build is UNIVERSAL — there is no generator to stamp a concrete <host> at package time, and the service address is an operator setting entered per profile. <all_urls> IS requested — SECURITY-SENSITIVE, deliberate — and now carries TWO paths, not one: (1) execute_js runs curator-supplied JS in the arbitrary http/https tabs this instance manages (§6), and (2) the startpage's fetch to the service /api/* is a cross-origin request whose CORS host-permission is <all_urls> (slice C / §12). Both are gated (execute_js by the per-copy options checkbox default OFF + the server kill-switch + commands.js's http/https-only edge guard; /api by a Bearer carrying the RAW per-install instance secret — option A, the server hashes it on receipt — over a wss:// address the client refuses to downgrade, src/service-address.js). A future tightening of <all_urls> to a fixed host list must therefore account for BOTH the execute_js origins AND the /api fetch host, or the startpage goes offline fleet-wide.",
  "host_permissions": [
    "<all_urls>"
  ],

  "background": {
    "service_worker": "src/service-worker.js",
    "type": "module"
  },

  "content_security_policy": {
    "extension_pages": "script-src 'self'; object-src 'self'"
  },

  "//chrome_url_overrides": "The newtab is the Vue 3 startpage built into startpage/ (offline-first quick links / search / jump / status bar, §10). All assets are local and the page runs under the extension_pages CSP `script-src 'self'` — the build uses base './' and precompiled SFCs (no runtime compiler), verified by the three §10 build gates.",
  "chrome_url_overrides": {
    "newtab": "startpage/index.html"
  },

  "options_page": "pages/options.html",

  "//icons": "Rendered from icons/icon.svg (the source of truth) with `rsvg-convert -w N -h N`. Chrome does NOT accept SVG for extension icons, so the PNGs are the shipped artefact and the SVG is kept only to re-render them. The 16px toolbar size is what the design was chosen for: it carries three bars, the last one detached to the right — a tab that moved elsewhere. Anything with more than three shapes turned to mush at 16px (measured, not guessed).",
  "icons": {
    "16": "icons/icon-16.png",
    "32": "icons/icon-32.png",
    "48": "icons/icon-48.png",
    "128": "icons/icon-128.png"
  },

  "//action": "The toolbar popup is 'Rule from this tab' (§8, §10): one click to add a rule <origin>/* -> this instance with a server-side impact preview.",
  "action": {
    "default_title": "arcextension",
    "default_popup": "pages/popup.html",
    "default_icon": {
      "16": "icons/icon-16.png",
      "32": "icons/icon-32.png"
    }
  },

  "incognito": "not_allowed"
}
