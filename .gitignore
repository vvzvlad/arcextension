.env
.venv/
venv/
ENV/
env/
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.swp
.DS_Store
logs/
data/*
!data/.gitkeep
# Claude Code agent worktrees — never commit
.claude/worktrees/

# Donor repo sits nested here — never pull it into this git
brave-window-mcp/

# Node / extension toolchain
extension/node_modules/
# Per-instance config carries EXT_TOKEN — never commit a real one (only the .example)
extension/instance.json
