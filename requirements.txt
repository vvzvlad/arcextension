pydantic-settings==2.7.0
loguru==0.7.2
httpx==0.28.1
starlette==1.3.1
uvicorn[standard]==0.52.1
# MCP server SDK (streamable HTTP), §11. Deliberately UNPINNED: the 307-redirect
# concern that once motivated pinning a version is false (the client hardcodes
# follow_redirects=True), so we track the SDK rather than freezing it.
mcp==2.0.0
